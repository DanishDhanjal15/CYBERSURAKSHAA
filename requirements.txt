# ═══════════════════════════════════════════════════════════════
# CYBERSURAKSHAA — All-in-One Detection Suite
# Combined requirements for all detection models
# Render-compatible: Python 3.10, CPU-only torch, EasyOCR
# ═══════════════════════════════════════════════════════════════

# ── Core Framework ────────────────────────────────────────────
Flask>=3.0.0
Werkzeug>=3.0.0
Pillow>=10.0.0
requests>=2.31.0
python-dotenv>=1.0.0
gunicorn>=21.2.0
flask-limiter>=3.5.0

# ── Fake Customer Care Detector ───────────────────────────────
# EasyOCR replaces PaddleOCR/PaddlePaddle — proper PyPI wheels for all platforms
easyocr>=1.7.0
spacy>=3.7.0,<3.8.0
numpy>=1.24.0,<2.0.0

# ── Deepfake Detector ────────────────────────────────────────
# CPU-only torch: no CUDA needed on cloud servers
torch==2.2.2
torchvision==0.17.2
timm>=1.0.0
facenet-pytorch>=2.5.3
# Use headless opencv — no GUI/display server needed on Render
opencv-python-headless>=4.8.0

# ── Betting Detector ─────────────────────────────────────────
scikit-learn>=1.5.0
ultralytics>=8.0.0

# ── Investment Scam Detector ─────────────────────────────────
python-whois>=0.9.4
xgboost>=2.0.3
transformers>=4.30.0
